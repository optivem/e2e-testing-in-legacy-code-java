spring:
  jackson:
    deserialization:
      ACCEPT_FLOAT_AS_INT: false
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5401/eshop}
    username: ${POSTGRES_USERNAME:eshop_user}
    password: ${POSTGRES_PASSWORD:eshop_password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: ${SERVER_PORT:8081}

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

allowed:
  origins: ${ALLOWED_ORIGINS:http://localhost:3001}

error:
  types:
    base-url: ${ERROR_TYPES_BASE_URL:https://api.optivem.com/errors}
    validation-error: ${error.types.base-url}/validation-error
    resource-not-found: ${error.types.base-url}/resource-not-found
    bad-request: ${error.types.base-url}/bad-request
    internal-server-error: ${error.types.base-url}/internal-server-error

erp:
  url: ${ERP_API_URL:http://localhost:9001/erp/api}
tax:
  url: ${TAX_API_URL:http://localhost:9001/tax/api}